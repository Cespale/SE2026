# 软件详细设计说明书

[TOC]

## 1 引言

### 1.1 编写目的

​		本文档为在线视频与直播网站的详细设计说明书，基于《软件概要设计说明书》和前期已作出的各模块详细设计，对各模块的内部逻辑、算法流程、接口定义及异常处理进行进一步细化，作为编码实现和配置项测试的直接依据。

​		本文档的预期读者为开发小组成员及课程教师与助教。开发人员可依据本文档进行详细设计和编码；课程教师和助教可依据本文档审查系统设计是否覆盖需求。

### 1.2 背景

- 待开发软件系统名称：在线视频与直播网站。

- 项目任务提出者：软件工程基础课程课程组。

- 开发者：软件工程基础课程9组。

- 用户：内容创作者、观看用户、平台运营管理人员。

- 运行环境：Web浏览器、服务器。

### 1.3 术语表

| 术语      | 说明                                           |
| :-------- | :--------------------------------------------- |
| JWT       | JSON Web Token，身份认证令牌                   |
| ORM       | Object-Relational Mapping，对象关系映射        |
| DTO       | Data Transfer Object，数据传输对象             |
| VO        | View Object，视图层对象                        |
| SPA       | Single Page Application，单页应用              |
| FFmpeg    | 开源音视频处理工具                             |
| SRS       | Simple Realtime Server，开源流媒体服务器       |
| RTMP      | Real-Time Messaging Protocol，实时消息传输协议 |
| WebSocket | 基于TCP的全双工通信协议                        |

### 1.4 参考文档

本文档参考了以下文档：

- 《软件工程基础2026春大作业要求》
- 《9组-软件开发计划书》
- 《9组-软件需求规格说明书》
- 《9组-软件概要设计说明书》
- 《软件详细设计说明书》文档模版

## 2 系统结构设计及子系统划分

本系统采用单体架构、前后端分离设计，系统子系统划分如下：

| 层         | 实现                                                 |
| ---------- | ---------------------------------------------------- |
| 表现层     | src/pages、src/components                            |
| 前端状态   | src/stores/authStore.ts、videoStore.ts、liveStore.ts |
| 接口层     | src/api.ts；backend/app/main.py                      |
| 认证与输入 | backend/app/security.py、schemas.py                  |
| 数据层     | backend/app/models.py、database.py、PostgreSQL       |

- 用户子系统：负责用户注册、登录、个人信息管理、关注关系维护、权限控制。

- 视频子系统：负责视频上传接收、分片合并、FFmpeg转码、多清晰度存储、播放地址生成、搜索查询。

- 直播子系统：负责直播间创建、推流地址分配、SRS回调处理、直播状态监控、结束清理。

- 弹幕子系统：负责视频弹幕持久化、直播弹幕实时广播、敏感词过滤、频率限制。

- 推荐子系统：负责用户兴趣分析、基于内容的推荐、热度计算、热门榜单维护。

- 互动子系统：负责评论发布与回复、点赞收藏、私信聊天、系统通知。

- 创作者子系统：负责内容管理、数据统计、粉丝互动工具。

- 管理子系统：负责内容审核、用户管理、举报处理、敏感词配置。

### 2.1 类图

系统核心领域类及其关系如下图所示：

![04-类图](.\第9组-StreamHub-图\04-类图.jpg)

### 2.2 主要领域类

| 类       | 职责                               | 表         |
| -------- | ---------------------------------- | ---------- |
| User     | 账户、密码散列、角色、资料         | users      |
| Category | 视频/直播分类                      | categories |
| Video    | 视频元数据、统计、上传者、审核状态 | videos     |
| Comment  | 视频评论、父评论关系               | comments   |
| Danmaku  | 视频或直播弹幕                     | danmaku    |
| LiveRoom | 直播间、主播、推拉流地址、状态     | live_rooms |

**状态值域约定**：

- `Video.audit_status`：0 待审核、1 通过、2 驳回
- `Video.status`：0 正常、1 已删除
- `LiveRoom.status`：1 进行中、2 已结束
- 视频列表默认只返回 `status=0`、`audit_status=1` 的记录；直播列表默认只返回 `status=1` 的记录。

## 3 功能模块设计

### 3.1 用户管理模块

模块编号：M001

模块名称：用户管理模块

输入：注册信息（账号、密码）、登录凭证（账号、密码）、个人信息修改数据

处理：

- 注册：校验账号唯一性，BCrypt加密密码，插入t_user，默认user_type=0（普通用户）
- 登录：校验账号密码，生成JWT令牌（有效期7天），返回token和用户信息
- 信息修改：校验身份，更新t_user对应字段，头像上传至文件目录
- 权限提升：普通用户申请创作者，管理员审核后更新user_type=1

算法描述：

- JWT生成：使用jjwt库，签名密钥配置于application.yml，包含userId、userType、exp
- 密码加密：BCryptPasswordEncoder，强度10

输出：用户信息VO、JWT令牌、操作结果

### 3.2 视频上传与处理模块

模块编号：M002

模块名称：视频上传与处理模块

输入：视频分片文件（multipart/form-data）、分片序号、总分片数、视频元信息（标题、描述、标签、分类、封面）

处理：

- 文件接收：直接接收完整视频文件（multipart/form-data）
- 文件保存：生成唯一文件名（用户ID_时间戳.mp4），保存至 `/app/public/uploads/videos/` 目录
- 封面生成：使用 OpenCV 读取视频第一帧，保存为 JPG 图片至 `/app/public/uploads/covers/` 目录
- 时长获取：通过 OpenCV 读取视频帧率和总帧数计算时长
- 入库：插入 videos 表，audit_status=0（待审核），status=0
- 同步处理：上传完成后立即返回（不进行异步转码）

输出：视频ID、转码状态、播放地址JSON

### 3.3 视频播放模块

模块编号：M003

模块名称：视频播放模块

输入：视频ID、用户ID（可选，用于记录历史）

处理：

- 校验视频状态：audit_status=1且status=0方可播放
- 拼接播放地址：根据视频ID查找文件目录，生成各清晰度URL
- 记录播放历史：登录用户插入或更新t_video_history，记录progress
- 返回弹幕数据：查询t_danmaku，target_id=videoId且target_type=0，限制前500条

输出：视频详情VO（含多清晰度URL列表）、弹幕列表、评论列表

### 3.4 直播管理模块

模块编号：M004

模块名称：直播管理模块

输入：直播间配置信息（标题、分类、封面）、主播ID

处理：

- 创建直播间：使用用户固定的 stream_key（6位数字），拼接 pushUrl（rtmp://服务器/live）和 pullUrl（http://服务器:8080/live/streamKey.flv），插入 live_rooms 表，status=1
- SRS 集成：SRS 作为独立容器运行，后端通过 HTTP API 管理流（可选）
- 结束直播：校验主播身份，删除直播间记录，关闭 WebSocket 连接
- 在线人数统计：通过 WebSocket 连接数实时统计，存储于内存中

输出：直播间信息、直播间列表、在线人数

### 3.5 直播弹幕模块

模块编号：M005

模块名称：直播弹幕模块

输入：WebSocket连接请求、弹幕消息JSON

处理：

- 连接建立：从URL解析roomId，校验token，将Session加入roomSessions
- 消息处理：
  - join类型：广播系统消息"xxx进入直播间"，返回join_ack含onlineCount
  - danmaku类型：频率限制，敏感词过滤
  - heartbeat类型：无需处理，保持连接
- 连接关闭：从roomSessions移除，更新onlineCount，广播系统消息

输出：广播消息（danmaku/system/online/error类型JSON）

### 3.6 视频弹幕模块

模块编号：M006

模块名称：视频弹幕模块

输入：弹幕内容（<=100字）、颜色、位置、视频时间点、用户ID、视频ID

处理：

- 校验：登录用户方可发送，内容长度限制
- 频率限制：前端限制每秒发送一条（暂未实现后端频率限制）
- 敏感词过滤：敏感词存储在 sensitive_words 表，启动时加载到内存缓存，包含敏感词的内容自动替换为 * 号（统一采用替换策略）
- 入库：插入t_danmaku
- 可选：同视频其他在线用户通过WebSocket或轮询获取实时弹幕

输出：发送结果、弹幕列表

### 3.7 推荐算法模块

模块编号：M007

模块名称：推荐算法模块

输入：用户ID（可选）、页码、每页大小、分类ID（可选）

处理：

- 主页推荐：基于播放量、点赞数和新视频加成的混合排序
  - 热度分 = 播放量 + 点赞数 × 2
  - 新视频加成：7天内发布的视频获得额外 200-600 分
  - 随机扰动：0-800 分，确保顺序有变化
- 相关视频推荐：
  - 优先展示相同分类的视频（按热度排序）
  - 不足时用其他分类的热门视频补充
  - 总数不超过 5 个，不同类视频随机排序

输出：推荐视频列表、热门榜单、相关视频列表

### 3.8 视频搜索模块

模块编号：M008

模块名称：视频搜索模块

输入：关键词、排序方式、页码

处理：

- SQL全文检索：MATCH(title, tags) AGAINST('关键词' IN NATURAL LANGUAGE MODE)
- 排序：综合=全文相关度+播放量倒序，最新=upload_time倒序，最热=view_count倒序
- 过滤：audit_status=1且status=0

输出：搜索结果列表（分页）

### 3.9 评论模块

模块编号：M009

模块名称：评论模块

输入：评论内容、视频ID、父评论ID（回复时为顶层评论ID，否则为0）、用户ID

处理：

- 发布评论：插入t_comment，parent_id=0为顶层评论，否则为回复
- 评论列表：按时间倒序查顶层评论，每条带前3条回复，可展开更多
- 点赞评论：t_comment.like_count++，记录点赞关系（可选）
- 置顶评论：创作者可设置is_top=1，置顶评论优先展示

输出：评论列表（含回复）、操作结果

### 3.10 关注与通知模块

模块编号：M010

模块名称：关注与通知模块

输入：关注者ID、被关注者ID

处理：

- 关注：插入t_follow，复合主键防重复
- 取消关注：删除t_follow记录
- 粉丝列表：查t_follow.following_id = 用户ID
- 关注列表：查t_follow.follower_id = 用户ID
- 系统通知：用户被关注/被点赞/被评论时，插入t_notification，用户查未读通知

输出：关注关系、粉丝列表、通知列表

### 3.11 创作者中心模块

模块编号：M011

模块名称：创作者中心模块

输入：创作者用户ID、时间范围

处理：

- 内容管理：查t_video.uploader_id = 创作者ID，展示审核状态，支持编辑删除
- 数据概览：统计总播放量、总粉丝数、总获赞数
- 趋势数据：近7天每日播放量，SQL按DATE(create_time)分组统计
- 粉丝管理：查粉丝列表，展示关注时间

输出：统计数据、视频列表、粉丝列表、趋势数据

### 3.12 平台管理模块

模块编号：M012

模块名称：平台管理模块

输入：管理员账号、审核操作、敏感词数据

处理：

- 视频审核：查t_video.audit_status=0，管理员点击通过/拒绝，更新audit_status
- 用户管理：查t_user列表，支持封禁/解封（更新status）
- 举报处理：查t_video_report.status=0，处理时更新status和handle_note
- 敏感词管理：增删改t_sensitive_word，修改后调用reload热更新

输出：审核列表、用户列表、举报列表、操作结果

### 3.12 用例对象级模型

本节针对需求规格说明书和概要设计中定义的8个核心业务用例，以对象级顺序图展示前端页面组件、Store状态管理、后端API端点和数据层领域类之间的交互时序，并标注对应的实现模块和测试编号，作为编码实现的直接依据。

**OBJ-SEQ01 发现并播放视频**

- 需求/用例：REQ01 / UC01
- 实现模块：HomePage.tsx、SearchPage.tsx、VideoPage.tsx；videoStore.fetchVideos/fetchVideoDetail；main.py:list_videos/get_video/related
- 关键对象：HomePage/SearchPage/VideoPage、useVideoStore、list_videos/get_video、Video。
- 测试编号：UNIT-TC01 / INT-TC01 / E2E-TC01

![OBJ-SEQ01-发现并播放视频](.\第9组-StreamHub-图\OBJ-SEQ01-发现并播放视频.jpg)

**OBJ-SEQ02 视频互动**

- 需求/用例：REQ02 / UC02
- 实现模块：VideoPage.tsx；videoStore.likeVideo/favoriteVideo/addComment/sendDanmaku；main.py:like_video/favorite_video/add_comment/add_danmaku
- 关键对象：VideoPage、useVideoStore、get_current_user、Video/Comment/Danmaku。
- 测试编号：UNIT-TC02 / INT-TC02 / E2E-TC02

![OBJ-SEQ02-视频互动](.\第9组-StreamHub-图\OBJ-SEQ02-视频互动.jpg)

**OBJ-SEQ03 创作者提交视频待审核**

- 需求/用例：REQ03 / UC03
- 实现模块：UploadPage.tsx；videoStore.uploadVideo；main.py:create_video；models.py:Video
- 关键对象：UploadPage、useVideoStore、require_creator、Video。
- 测试编号：UNIT-TC03 / INT-TC03 / E2E-TC03

![OBJ-SEQ03-创作者提交视频待审核](.\第9组-StreamHub-图\OBJ-SEQ03-创作者提交视频待审核.jpg)

**OBJ-SEQ04 管理员审核视频**

- 需求/用例：REQ04 / UC04
- 实现模块：AdminPage.tsx；videoStore.fetchPendingVideos/auditVideo；main.py:pending_videos/audit_video
- 关键对象：AdminPage、useVideoStore、require_admin、Video。
- 测试编号：UNIT-TC04 / INT-TC04 / E2E-TC04

![OBJ-SEQ04-管理员审核视频](.\第9组-StreamHub-图\OBJ-SEQ04-管理员审核视频.jpg)

**OBJ-SEQ05 创作者管理本人作品**

- 需求/用例：REQ05 / UC05
- 实现模块：CreatorPage.tsx；videoStore.fetchCreatorVideos；main.py:creator_videos
- 关键对象：CreatorPage、useVideoStore、require_creator、Video。
- 测试编号：UNIT-TC05 / INT-TC05 / E2E-TC05

![OBJ-SEQ05-创作者管理本人作品](.\第9组-StreamHub-图\OBJ-SEQ05-创作者管理本人作品.jpg)

**OBJ-SEQ06 创作者创建直播间**

- 需求/用例：REQ06 / UC06
- 实现模块：LiveStartPage.tsx；liveStore.createRoom；main.py:create_room；models.py:LiveRoom
- 关键对象：LiveStartPage、useLiveStore、require_creator、LiveRoom。
- 测试编号：UNIT-TC06 / INT-TC06 / E2E-TC06

![OBJ-SEQ06-创作者创建直播间](.\第9组-StreamHub-图\OBJ-SEQ06-创作者创建直播间.jpg)

**OBJ-SEQ07 进入直播并实时互动**

- 需求/用例：REQ07 / UC07
- 实现模块：LivePage.tsx；liveStore.connectWebSocket/sendDanmaku；main.py:list_rooms/room_detail/live_ws；LiveHub
- 关键对象：LivePage、useLiveStore、room_detail/live_ws、LiveRoom、LiveHub。
- 测试编号：UNIT-TC07 / INT-TC07 / E2E-TC07

![OBJ-SEQ07-进入直播间并实时互动](.\第9组-StreamHub-图\OBJ-SEQ07-进入直播间并实时互动.jpg)

**OBJ-SEQ08 创作者结束直播**

- 需求/用例：REQ08 / UC08
- 实现模块：LivePage.tsx；liveStore.endRoom；main.py:end_room；models.py:LiveRoom
- 关键对象：LivePage、useLiveStore、require_creator、LiveRoom。
- 测试编号：UNIT-TC08 / INT-TC08 / E2E-TC08

![OBJ-SEQ08-创作者结束直播](.\第9组-StreamHub-图\OBJ-SEQ08-创作者结束直播.jpg)

## 4 界面设计

### 4.1 外部界面设计

本系统外部接口包括RESTful API和WebSocket接口。

RESTful API返回格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

错误码定义：

- 0：成功
- 400：参数校验失败
- 401：未授权/Token无效
- 403：权限不足
- 404：资源不存在
- 500：服务器内部错误
- 1001：账号或密码错误
- 1002：账号已存在

### 4.2 内部界面设计

模块间通过Service层方法调用交互，不直接跨模块访问Mapper。

- UserService为其他模块提供getById、getCurrentUser方法
- VideoService为RecommendService提供getByIds方法
- LiveRoomService为WebSocketHandler提供getByStreamKey、updateOnlineCount方法

### 4.3 用户界面设计

用户界面采用 React 18 + TypeScript + Tailwind CSS 构建，SPA 单页应用，使用 React Router 管理路由，Zustand 管理状态。

视频播放页：左侧原生 HTML5 video 播放器（16:9），支持弹幕层（Framer Motion 动画），右侧评论区。

主要页面：

- 首页：分类标签栏 + 视频卡片网格（响应式5/4/3/2列）+ 无限滚动加载
- 视频播放页：左侧播放器（原生 HTML5 video 播放器，16:9）+ 右侧UP主信息及相关视频，下方评论区
- 直播间页：左侧直播播放器（flv.js）+ 右侧聊天面板（WebSocket实时）
- 上传页：文件选择；分片上传；信息填写
- 创作者中心：左侧边栏菜单 + 右侧数据面板
- 管理后台：左侧边栏菜单 + 右侧表格操作

## 5 异常处理设计

### 5.1 出错信息管理

异常分类：

- 业务异常（BizException）：已知错误，如"视频不存在"，返回对应错误码和消息
- 参数校验异常（MethodArgumentNotValidException）：返回400，附带具体字段错误
- 认证异常：返回401，前端跳转登录
- 系统异常：返回500，记录错误日志，不暴露内部信息

日志记录：ERROR级别记录堆栈，INFO级别记录关键操作。

### 5.2故障预防与补救

- 数据库连接失败：自动重连，失败时返回"服务暂不可用"，记录日志
- 文件上传中断：支持分片重传，合并时校验缺失分片
- WebSocket断连：前端自动重连，重连失败提示刷新页面

### 5.3 系统维护设计

- 数据库备份：手动备份
- 日志轮转：按天切割日志文件，保留一定时间

## 6 性能优化和安全设计

### 6.1 性能优化

- 数据库：核心表建立索引（用户账号、视频上传者、创建时间），减少全表扫描
- 分页：所有列表接口强制分页（每页 20 条）
- 静态资源：通过 FastAPI StaticFiles 中间件直接提供视频和封面文件
- Docker Volume：使用 Docker Volume 持久化上传文件和数据库数据

### 6.2 安全设计

- 密码：BCrypt加密存储，不保存明文
- JWT：请求头Authorization: Bearer {token}，后端校验签名和有效期
- SQL注入：采用参数化查询
- 文件上传：限制类型（video/mp4,video/avi,video/quicktime），限制大小（2GB），存储目录无执行权限
- 敏感数据：脱敏处理密码、token、验证码

## 7 项目测试计划

| 序号 | 测试类型   | 对应部分                 | 测试内容                              | 计划测试时间   |
| :--: | :--------- | :----------------------- | :------------------------------------ | :------------- |
|  1   | 单元测试   | 所有Service方法          | 覆盖率≥80%                            | 编码阶段       |
|  2   | 接口测试   | 所有Controller           | Postman集合，正常+异常场景            | 相应模块完成后 |
|  3   | 集成测试   | 视频上传-转码-播放全流程 | 端到端流程验证                        | 相应编码完成后 |
|  4   | 性能测试   | 推荐接口、搜索接口       | 模拟一定数量并发                      | 测试阶段       |
|  5   | 安全测试   | 登录、权限、上传         | 渗透测试基础项                        | 测试阶段       |
|  6   | 兼容性测试 | 前端页面                 | Chrome/Firefox/Edge等较新版本访问效果 | 测试阶段       |
