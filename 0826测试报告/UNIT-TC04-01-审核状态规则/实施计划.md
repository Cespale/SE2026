# UNIT-TC04-01 审核状态规则 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用一个可重复运行的单元测试限制管理员审核视频时只能提交“通过(1)”或“驳回(2)”。

**Architecture:** 测试直接实例化 `AuditIn`，不启动 FastAPI、不访问数据库。Pydantic 模型承担输入边界校验；`Literal[1, 2]` 是最小生产修改，非法状态在路由写数据库前被拒绝。

**Tech Stack:** Python 3.11、pytest、Pydantic 2.10。

## Global Constraints

- 测试源码必须位于 `StreamHub/backend/tests/`，否则 pytest 与后续 CI 不会发现它。
- 所有用户可查阅的说明、日志、JUnit、门禁记录写入 `0826测试报告/UNIT-TC04-01-审核状态规则/`。
- 不修改 API、E2E、Docker、数据库模式；不添加依赖。
- 不触碰仓库已有的 E2E 失败证据和未提交文件。

---

### Task 1: Pydantic 审核状态边界

**Files:**
- Create: `C:/Users/lausu/Desktop/大三上学期/大二小学期/StreamHub/backend/tests/test_audit_rules_unit.py`
- Modify: `C:/Users/lausu/Desktop/大三上学期/大二小学期/StreamHub/backend/app/schemas.py:2,116`
- Evidence: `C:/Users/lausu/Desktop/大三上学期/大二小学期/0826测试报告/UNIT-TC04-01-审核状态规则/证据/`

**Interfaces:**
- Consumes: `app.schemas.AuditIn(auditStatus: int)`。
- Produces: `AuditIn.auditStatus` 仅可为 `1` 或 `2`；其他整数抛出 `pydantic.ValidationError`。

- [x] **Step 1: 写失败测试**

```python
import pytest
from pydantic import ValidationError

from app.schemas import AuditIn


def test_audit_status_allows_final_decisions_only():
    assert AuditIn(auditStatus=1).auditStatus == 1
    assert AuditIn(auditStatus=2).auditStatus == 2

    for invalid_status in (0, 3, -1):
        with pytest.raises(ValidationError) as error:
            AuditIn(auditStatus=invalid_status)

        assert error.value.errors()[0]["type"] == "literal_error"
```

- [x] **Step 2: 运行并确认 RED**

```powershell
cd SE2026-main\backend
.\.venv\Scripts\python.exe -m pytest --noconftest tests\test_audit_rules_unit.py -q
```

预期：失败，`0` 未抛出 `ValidationError`。

- [x] **Step 3: 最小生产修改**

```python
from typing import List, Literal, Optional

class AuditIn(BaseModel):
    auditStatus: Literal[1, 2]
```

- [x] **Step 4: 运行 GREEN 与后端回归**

```powershell
.\.venv\Scripts\python.exe -m pytest --noconftest tests\test_audit_rules_unit.py -q --junitxml="SE2026-main\0826测试报告\UNIT-TC04-01-审核状态规则\证据\unit-tc04-01.xml"
.\.venv\Scripts\python.exe -m pytest tests -q --junitxml="SE2026-main\0826测试报告\UNIT-TC04-01-审核状态规则\证据\backend-regression.xml"
```

预期：新单测通过；完整后端测试无失败。

### Task 2: 可审计证据包

**Files:**
- Create: `run-unit-tc04-gate.ps1`、`说明.md`、`证据/red.log`、`证据/pass.log`、`证据/backend-regression.log`、`证据/unit-tc04-01.xml`、`流水线门禁记录.md`

- [x] **Step 1: 创建本地门禁脚本；测试命令非零退出时输出 `PUBLISH_IMAGE=SKIPPED` 与 `DEPLOY=SKIPPED` 并立即退出。**

```powershell
& $python -m pytest --noconftest tests/test_audit_rules_unit.py -q
if ($LASTEXITCODE -ne 0) {
  'PUBLISH_IMAGE=SKIPPED'
  'DEPLOY=SKIPPED'
  exit $LASTEXITCODE
}
```

- [x] **Step 2: 保存 RED、GREEN、完整后端回归原始输出。**
- [x] **Step 3: 说明测试目标、过程、断言、结果、证据路径及运行环境。**
- [x] **Step 4: 明确本地门禁失败会停止后续发布/部署；托管 CI 尚未推送运行，不伪造远程流水线记录。**
