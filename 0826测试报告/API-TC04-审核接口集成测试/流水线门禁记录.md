# API-TC04 流水线门禁记录

## 门禁代码

`run-api-tc04-gate.ps1` 先运行传入的 pytest 目标。

- 测试退出码非零：输出 `API_TEST=FAILED`、`PUBLISH_IMAGE=SKIPPED`、`DEPLOY=SKIPPED`，并以同一非零退出码退出。
- 测试通过：输出 `API_TEST=PASSED`；本次没有配置镜像发布和部署动作，因此记录 `PUBLISH_IMAGE=NOT_RUN`、`DEPLOY=NOT_RUN`。

## 已执行记录

| 运行 | 测试 | 镜像发布 | 部署 | 退出码 | 证据 |
|---|---|---|---|---:|---|
| 受控回归失败 | FAILED | SKIPPED | SKIPPED | 1 | `证据/pipeline-controlled-regression-failed.log` |
| 完整 API 测试 | 6 passed | NOT_RUN | NOT_RUN | 0 | `证据/pipeline-full-api-passed.log` |

## 事实边界

这是本地可重复门禁记录。没有新增、推送或执行 GitHub Actions，也没有发布镜像或部署，因此不能称为远程流水线通过。

正式 CI 后续应设定：API 测试 job 成功后才允许镜像 job；镜像 job 成功后才允许部署 job。应保存平台运行编号、链接、失败日志和成功日志。
