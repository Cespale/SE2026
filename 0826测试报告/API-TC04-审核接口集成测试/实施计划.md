# API-TC04 审核接口集成测试 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用真实 HTTP 请求、FastAPI 路由、认证和 PostgreSQL 测试库验证视频审核 API 的成功、备选和异常流程。

**Architecture:** 复用 `backend/tests/conftest.py` 的 `TestClient` 与 `streamhub_test` 自动重建数据库。复用现有成功、驳回、无权限测试；只追加两个缺失异常断言：非法审核状态返回 `422` 且数据库不变，不存在视频返回 `404`。

**Tech Stack:** Python 3.11、pytest、FastAPI 0.115.6、Pydantic 2.10.4、SQLAlchemy、PostgreSQL。

## Global Constraints

- 用户已明确授权直接修改 `StreamHub` 当前 `main`；仓库已有未提交文件，除列明文件外不得改动。
- 测试只连接 `streamhub_test`；fixture 已检查数据库名并每例删建表，不访问 `streamhub` 演示库。
- 不新增依赖，不改生产代码；`AuditIn` 的 `Literal[1, 2]` 是第一类单元测试已有修复。
- 说明、报告、JUnit、日志、门禁脚本都写入此目录；可执行 pytest 测试源码必须保留在项目 `backend/tests/`。
- 不伪造托管 CI、镜像发布或部署记录；本次只生成可重复的本地门禁记录。

---

### Task 1: 补齐审核 API 异常分支

**Files:**
- Modify: `C:/Users/lausu/Desktop/大三上学期/大二小学期/StreamHub/backend/tests/test_video_flow_api.py`
- Existing production dependency: `C:/Users/lausu/Desktop/大三上学期/大二小学期/StreamHub/backend/app/main.py:848-854`
- Existing validation dependency: `C:/Users/lausu/Desktop/大三上学期/大二小学期/StreamHub/backend/app/schemas.py:115-116`

**Interfaces:**
- `PATCH /api/admin/videos/{video_id}/audit` with administrator bearer token.
- Legal body: `{ "auditStatus": 1 }` or `{ "auditStatus": 2 }`.
- Invalid body `{ "auditStatus": 0 }` returns `422`; nonexistent valid UUID returns `404`.

- [x] **Step 1: Add two API tests.**

```python
def test_admin_cannot_return_submitted_video_to_pending(client):
    creator_headers = login_headers(client, "creator", "creator123")
    admin_headers = login_headers(client, "admin", "admin123")
    create_response = client.post(
        "/api/videos",
        headers=creator_headers,
        json={"title": "pytest 非法审核状态", "categoryId": "1"},
    )
    assert create_response.status_code == 200
    video_id = create_response.json()["id"]

    response = client.patch(
        f"/api/admin/videos/{video_id}/audit",
        headers=admin_headers,
        json={"auditStatus": 0},
    )
    assert response.status_code == 422
    assert response.json()["detail"][0]["loc"] == ["body", "auditStatus"]
    assert response.json()["detail"][0]["type"] == "literal_error"

    pending_response = client.get("/api/admin/videos/pending", headers=admin_headers)
    assert pending_response.status_code == 200
    pending_video = next(v for v in pending_response.json()["items"] if v["id"] == video_id)
    assert pending_video["auditStatus"] == 0


def test_admin_cannot_audit_nonexistent_video(client):
    admin_headers = login_headers(client, "admin", "admin123")
    response = client.patch(
        "/api/admin/videos/00000000-0000-0000-0000-000000000001/audit",
        headers=admin_headers,
        json={"auditStatus": 1},
    )
    assert response.status_code == 404
    assert response.json() == {"detail": "视频不存在"}
```

- [x] **Step 2: Run target file.**

```powershell
cd SE2026-main\backend
.\.venv\Scripts\python.exe -m pytest tests\test_video_flow_api.py -q
```

Expected: six API tests pass. They exercise login, role dependency, video creation, query/update, transaction commit and response serialization against `streamhub_test`.

### Task 2: 回归证明、门禁与报告

**Files:**
- Create: `C:/Users/lausu/Desktop/大三上学期/大二小学期/0826测试报告/API-TC04-审核接口集成测试/run-api-tc04-gate.ps1`
- Create: `C:/Users/lausu/Desktop/大三上学期/大二小学期/0826测试报告/API-TC04-审核接口集成测试/测试说明.md`
- Create: `C:/Users/lausu/Desktop/大三上学期/大二小学期/0826测试报告/API-TC04-审核接口集成测试/测试报告.md`
- Create: `C:/Users/lausu/Desktop/大三上学期/大二小学期/0826测试报告/API-TC04-审核接口集成测试/流水线门禁记录.md`

- [x] **Step 1: Controlled regression proof.**

Temporarily change only `AuditIn.auditStatus` from `Literal[1, 2]` to `int`, run `test_admin_cannot_return_submitted_video_to_pending`, and confirm its `assert response.status_code == 422` fails because API accepts `0`. Restore `Literal[1, 2]` immediately through an explicit patch, then rerun. This is a controlled proof, not a current product failure.

- [x] **Step 2: Run local gate.**

```powershell
param([string]$Python, [string]$TestPath)
& $Python -m pytest $TestPath -q
$testExitCode = $LASTEXITCODE
if ($testExitCode -ne 0) {
  'API_TEST=FAILED'
  'PUBLISH_IMAGE=SKIPPED'
  'DEPLOY=SKIPPED'
  exit $testExitCode
}
'API_TEST=PASSED'
'PUBLISH_IMAGE=NOT_RUN'
'DEPLOY=NOT_RUN'
```

- [x] **Step 3: Generate evidence.**

Run final target file with JUnit, then `pytest tests -q` with JUnit. Record totals, pass/fail/error, controlled regression cause, environment, test code address, changed-code address and test-database boundary.

- [x] **Step 4: Update first report code addresses.**

Confirm `UNIT-TC04-01-审核状态规则/测试报告.md` lists test, production-change and gate-script paths.
