# StreamHub 

# 微服务划分图、接口清单与数据表归属方案

## 一、业务场景（用例）清单

### 1. 用户相关用例
- **UC01**: 用户发现并播放视频
- **UC10**: 用户查看个人主页
- **UC11**: 用户修改个人资料
- **UC12**: 用户修改密码
- **UC13**: 普通用户申请成为创作者
- **UC14**: 用户发送与接收私信
- **UC17**: 管理者管理用户

### 2. 内容相关用例
- **UC01**: 用户发现并播放视频
- **UC03**: 创作者提交视频
- **UC04**: 管理员审核视频
- **UC05**: 创作者管理本人作品
- **UC09**: 用户查看订阅动态
- **UC15**: 管理者管理视频

### 3. 社交相关用例
- **UC02**: 用户视频互动
- **UC06**: 创作者创建直播间
- **UC07**: 进入直播并实时互动
- **UC08**: 创作者结束直播
- **UC16**: 管理者管理直播

### 4. 管理相关用例
- **UC18**: 管理者管理敏感词

## 二、微服务划分图

### 1. 微服务架构图

![微服务总体架构图](.\第9组-StreamHub-图\微服务总体架构图.jpg)

### 2. 微服务划分图

#### 2.1 用户服务

![用户服务](.\第9组-StreamHub-图\用户服务.jpg)

#### 2.2 内容服务

![内容服务](.\第9组-StreamHub-图\内容服务.jpg)

#### 2.3 社交服务

![社交服务](.\第9组-StreamHub-图\社交服务.jpg)

## 三、服务接口清单

### 1. 用户服务
**负责用户管理、认证、私信等核心用户功能**

#### 1.1 认证与安全
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| POST | `/auth/register` | 用户注册 | `{account, password, nickname}` | `{user_id, token, user_info}` | 201 |
| POST | `/auth/login` | 用户登录 | `{account, password}` | `{token, user_info}` | 200 |
| GET | `/auth/me` | 获取当前用户信息 | Header: `Authorization: Bearer token` | `{user_info}` | 200 |
| PATCH | `/auth/me` | 更新个人资料 | `{nickname, avatar, bio}` | `{user_info}` | 200 |
| POST | `/auth/refresh` | 刷新 token | `{refresh_token}` | `{access_token}` | 200 |
| POST | `/auth/logout` | 用户登出 | Header: `Authorization: Bearer token` | `{message: "success"}` | 200 |
| PUT | `/auth/change-password` | 修改密码 | `{old_password, new_password}` | `{code, message}` | 200 |
| POST | `/auth/upgrade-to-creator` | 申请成为创作者 | - | `{code, message, data}` | 200 |
| POST | `/auth/upload-avatar` | 上传头像 | File: avatar | `{code, message, data}` | 200 |

#### 1.2 用户信息管理
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/users/{user_id}` | 获取用户基本信息 | - | `{user_info}` | 200 |
| GET | `/users/{user_id}/videos` | 获取用户视频列表 | `page, page_size` | `{items, total}` | 200 |
| GET | `/users/{user_id}/stats` | 获取用户统计信息 | - | `{followerCount, followingCount, likeCount}` | 200 |

#### 1.3 私信功能 (UC14)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/messages/conversations` | 获取会话列表 | - | `[{conversations}]` | 200 |
| POST | `/messages/conversations` | 创建会话 | `{peer_id}` | `{conversation_info}` | 201 |
| GET | `/messages/conversations/{conv_id}` | 获取会话详情 | - | `{conversation_info}` | 200 |
| GET | `/messages/conversations/{conv_id}/messages` | 获取消息列表 | `limit, before` | `[{messages}]` | 200 |
| POST | `/messages/conversations/{conv_id}/messages` | 发送消息 | `{content, type}` | `{message_info}` | 201 |
| DELETE | `/messages/{msg_id}` | 删除消息 | - | `{code, message}` | 200 |
| PATCH | `/messages/{msg_id}/read` | 标记已读 | - | `{ok: true}` | 200 |

#### 1.4 用户管理 (UC17)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/admin/users` | 获取用户列表 | `page, limit, keyword` | `{items, total, page, limit}` | 200 |
| PATCH | `/admin/users/{user_id}/type` | 修改用户类型 | `{user_type}` | `{user_info}` | 200 |
| PATCH | `/admin/users/{user_id}/status` | 封禁/解封用户 | `{status}` | `{user_info}` | 200 |

### 2. 内容服务
**负责视频、直播、内容审核等核心内容功能**

#### 2.1 视频发现 (UC01)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/videos` | 获取视频列表 | `page, size, category_id, keyword, sort` | `{items, has_more}` | 200 |
| GET | `/videos/{video_id}` | 获取视频详情 | - | `{video_info}` | 200 |
| GET | `/videos/{video_id}/stream` | 获取视频流 | - | Video流 | 200 |
| GET | `/videos/recommended` | 获取推荐视频 | `page, size, user_id` | `{items, has_more}` | 200 |
| GET | `/videos/related` | 获取相关视频 | `video_id` | `{items}` | 200 |

#### 2.2 视频管理 (UC03, UC05)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| POST | `/videos` | 上传视频 | `{title, description, category_id}` | `{video_info}` | 201 |
| PUT | `/videos/{video_id}` | 更新视频信息 | `{title, description, category_id}` | `{video_info}` | 200 |
| DELETE | `/videos/{video_id}` | 删除视频 | - | `{code, message}` | 200 |
| POST | `/videos/{video_id}/upload` | 上传视频文件 | File: video | `{url, duration, cover}` | 201 |
| POST | `/videos/{video_id}/upload-cover` | 上传封面 | File: cover | `{url}` | 201 |
| GET | `/creator/videos` | 获取创作者视频 | `page, size, status` | `{items, total}` | 200 |

#### 2.3 视频审核 (UC04, UC15)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/admin/videos/pending` | 获取待审核视频 | `page, size` | `{items, total}` | 200 |
| PATCH | `/admin/videos/{video_id}/approve` | 通过审核 | - | `{code, message}` | 200 |
| PATCH | `/admin/videos/{video_id}/reject` | 驳回视频 | `{reason}` | `{code, message}` | 200 |
| PATCH | `/admin/videos/{video_id}/warn` | 警告视频 | `{reason}` | `{code, message}` | 200 |
| GET | `/admin/videos` | 获取所有视频 | `page, size, keyword, status` | `{items, total}` | 200 |
| DELETE | `/admin/videos/{video_id}` | 删除视频 | - | `{code, message}` | 200 |

#### 2.4 订阅动态 (UC09)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/feed` | 获取关注动态 | `page, size` | `{items, has_more}` | 200 |
| POST | `/follow/{user_id}` | 关注用户 | - | `{success}` | 200 |
| DELETE | `/follow/{user_id}` | 取消关注 | - | `{success}` | 200 |
| GET | `/following` | 获取关注列表 | `page, size` | `{items}` | 200 |
| GET | `/followers` | 获取粉丝列表 | `page, size` | `{items}` | 200 |

#### 2.5 分类管理
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/categories` | 获取分类列表 | - | `[{id, name, type}]` | 200 |
| POST | `/categories` | 创建分类 | `{name, type, sort_order}` | `{category_info}` | 201 |
| PUT | `/categories/{id}` | 更新分类 | `{name, sort_order}` | `{category_info}` | 200 |
| DELETE | `/categories/{id}` | 删除分类 | - | `{code, message}` | 200 |

### 3. 社交服务
**负责互动功能、直播、实时通信等社交功能**

#### 3.1 视频互动 (UC02)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| POST | `/videos/{video_id}/like` | 点赞视频 | - | `{success, count}` | 200 |
| DELETE | `/videos/{video_id}/like` | 取消点赞 | - | `{success, count}` | 200 |
| GET | `/videos/{video_id}/likes` | 获取点赞状态 | - | `{liked, count}` | 200 |
| POST | `/videos/{video_id}/favorite` | 收藏视频 | - | `{success}` | 200 |
| DELETE | `/videos/{video_id}/favorite` | 取消收藏 | - | `{success}` | 200 |
| GET | `/videos/{video_id}/favorites` | 获取收藏状态 | - | `{favorited}` | 200 |
| POST | `/videos/{video_id}/comment` | 发表评论 | `{content, parent_id, reply_to}` | `{comment_info}` | 201 |
| GET | `/videos/{video_id}/comments` | 获取评论列表 | `page, size` | `{items, total}` | 200 |
| GET | `/comments/{id}/replies` | 获取回复列表 | `page, size` | `{items}` | 200 |
| DELETE | `/comments/{id}` | 删除评论 | - | `{code, message}` | 200 |
| POST | `/videos/{video_id}/danmaku` | 发送弹幕 | `{content, color, position, time}` | `{danmaku_info}` | 201 |
| GET | `/videos/{video_id}/danmaku` | 获取弹幕列表 | `time, limit` | `{items}` | 200 |

#### 3.2 直播功能 (UC06, UC07, UC08, UC16)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| POST | `/live/rooms` | 创建直播间 | `{title, category_id, description}` | `{room_info}` | 201 |
| GET | `/live/rooms` | 获取直播间列表 | `category_id, page, size, status` | `{items, total}` | 200 |
| GET | `/live/rooms/{id}` | 获取直播间详情 | - | `{room_info}` | 200 |
| POST | `/live/rooms/{id}/start` | 开始直播 | - | `{stream_key}` | 200 |
| POST | `/live/rooms/{id}/end` | 结束直播 | - | `{code, message}` | 200 |
| WS | `/live/{room_id}/ws` | 直播WebSocket | - | 实时消息 | - |
| POST | `/live/{room_id}/danmaku` | 发送直播弹幕 | `{content, color, position}` | `{code, message}` | 200 |
| GET | `/live/{room_id}/danmaku` | 获取直播弹幕 | `time, limit` | `{items}` | 200 |
| POST | `/live/rooms/{id}/gift` | 赠送礼物 | `{gift_id, count}` | `{code, message}` | 200 |

#### 3.3 举报管理
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| POST | `/reports` | 提交举报 | `{target_type, target_id, reason}` | `{code, message}` | 201 |
| GET | `/admin/reports` | 获取举报列表 | `page, size, status` | `{items, total}` | 200 |
| PATCH | `/admin/reports/{id}/handle` | 处理举报 | `{action, reason}` | `{code, message}` | 200 |
| PATCH | `/admin/reports/{id}/ignore` | 忽略举报 | - | `{code, message}` | 200 |

#### 3.4 敏感词管理 (UC18)
| HTTP 方法 | 路径 | 功能描述 | 请求参数 | 响应数据 | 状态码 |
|-----------|------|----------|----------|----------|--------|
| GET | `/admin/words` | 获取敏感词列表 | `page, size` | `{items, total}` | 200 |
| POST | `/admin/words` | 添加敏感词 | `{word}` | `{code, message, data}` | 201 |
| PUT | `/admin/words/{id}` | 更新敏感词 | `{word}` | `{code, message}` | 200 |
| DELETE | `/admin/words/{id}` | 删除敏感词 | - | `{code, message}` | 200 |
| POST | `/admin/words/check` | 检查内容 | `{content}` | `{has_sensitive, words}` | 200 |

## 四、数据表归属方案

### 1. 用户服务

| 数据表 | 表用途 | 关联用例 | 服务内访问 | 跨服务访问 |
|--------|--------|----------|------------|------------|
| **users** | 用户基本信息 | UC10, UC11, UC12, UC13, UC14, UC17 | CRUD | 内容服务查询用户名、头像 |
| **user_sessions** | 用户会话管理 | UC10, UC11, UC12, UC13, UC14, UC17 | 创建/查询/删除 | - |
| **messages** | 私信消息 | UC14 | CRUD | - |
| **conversations** | 私信会话 | UC14 | CRUD | - |
| **follows** | 关注关系 | UC09 | CRUD | 内容服务查询关注状态 |

### 2. 内容服务

| 数据表 | 表用途 | 关联用例 | 服务内访问 | 跨服务访问 |
|--------|--------|----------|------------|------------|
| **videos** | 视频信息 | UC01, UC03, UC04, UC05, UC09, UC15 | CRUD | 社交服务查询、互动 |
| **categories** | 分类信息 | UC01, UC03, UC05, UC09 | CRUD | - |
| **video_tags** | 视频标签 | UC01, UC03, UC05 | CRUD | - |
| **audit_logs** | 审核日志 | UC04, UC15 | 创建/查询 | - |
| **feed** | 订阅动态缓存 | UC09 | 查询/更新 | - |

### 3. 社交服务

| 数据表 | 表用途 | 关联用例 | 服务内访问 | 跨服务访问 |
|--------|--------|----------|------------|------------|
| **video_likes** | 点赞记录 | UC02 | CRUD | 内容服务更新计数 |
| **comments** | 评论记录 | UC02 | CRUD | - |
| **video_favorites** | 收藏记录 | UC02 | CRUD | - |
| **danmaku** | 弹幕记录 | UC02, UC07 | CRUD | - |
| **live_rooms** | 直播间 | UC06, UC07, UC08, UC16 | CRUD | - |
| **reports** | 举报记录 | UC02, UC16 | CRUD | - |
| **sensitive_words** | 敏感词 | UC02, UC18 | CRUD | 评论时过滤 |

### 4. 共享数据访问

#### 4.1 用户信息查询
- 内容服务 → 用户服务：通过HTTP调用获取用户基本信息
- 社交服务 → 用户服务：通过HTTP调用获取用户基本信息用于显示

#### 4.2 视频状态更新
- 社交服务 → 内容服务：通过事件更新点赞数、评论数、收藏数
- 使用消息队列异步更新，保证最终一致性

#### 4.3 实时数据
- 弹幕、评论、点赞：WebSocket实时推送
- 直播间状态：WebSocket实时推送在线人数等

## 五、跨服务调用策略

### 1. 同步调用（HTTP）
- 用户信息查询（GET /users/{user_id}）
- 视频基本信息查询（GET /videos/{video_id}）

### 2. 异步调用（消息队列）
- 点赞事件：用户点赞后发送事件，内容服务更新计数
- 评论事件：评论发送后通知相关用户
- 关注事件：用户关注后更新订阅动态

## 六、API网关路由

```
/api/auth/*          -> 用户服务
/api/users/*         -> 用户服务
/api/messages/*      -> 用户服务
/api/videos/*        -> 内容服务
/api/feed/*          -> 内容服务
/api/categories/*    -> 内容服务
/api/live/*          -> 社交服务
/api/like/*          -> 社交服务
/api/comment/*       -> 社交服务
/api/danmaku/*       -> 社交服务
/api/admin/*         -> 根据具体模块路由
```

## 七、服务边界说明

1. **用户服务边界**
   - 负责所有用户相关的数据和业务逻辑
   - 提供用户信息的查询接口供其他服务调用
   - 不负责视频、直播等核心内容业务

2. **内容服务边界**
   - 负责视频、分类、审核等核心内容业务
   - 维护视频的统计数据（播放量、点赞数等）
   - 不处理用户的实时互动（如评论、弹幕）

3. **社交服务边界**
   - 负责所有的互动功能（点赞、评论、弹幕、直播等）
   - 处理实时通信（WebSocket）
   - 不负责核心内容的存储和管理
