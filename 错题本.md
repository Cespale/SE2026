# StreamHub 错题本

仅记录已验证纠错和有效方法。创建日期：2026-08-24。

## Docker 拉取失败先分离网络问题和项目问题

- 纠错：`x509: certificate signed by unknown authority` 不能直接归因于 Dockerfile。
- 有效方法：先单独执行 `docker pull hello-world`，再根据完整报错区分原因：`x509` 偏向代理/证书信任；No proxy 后若变为 `dial tcp [IPv6 地址]:443` 超时，则是到 Docker Hub 的 IPv6 网络路径不可达，不是项目镜像名写错。
- Docker Desktop 的 DNS/IPv6 过滤设置不一定影响镜像拉取，因为拉取可能由 Containers Proxy 处理；不能只看设置页面，必须以新的 `docker pull hello-world` 结果为准。
- 每次改网络、代理或 Docker 设置后，只测一次 `docker pull hello-world`；它成功后，才继续 Compose。不要以关闭 TLS 校验或配置不安全镜像仓库来绕过问题。
- 补充：Compose 输出已经点名失败镜像时，先按失败阶段定位。此次 `ossrs/srs:5` 的 `x509` 发生在拉取阶段，与数据库位于本机还是组长机器无关；不能因为系统同时依赖数据库就把所有启动失败都归因于数据库。

## 用户只要本地启动时，不强制先解决完整 Docker

- 纠错：前后端可用 IDE/本机开发服务器启动；数据库仍需独立运行。
- 有效方法：先检查 `docker ps -a`、端口和 Compose 的 `container_name`。已有健康数据库时优先复用，再启动 Uvicorn 和前端；固定容器名会与其他项目冲突，不能未经确认删除旧容器或数据卷。
- 后端虚拟环境路径也要先确认存在；PowerShell 使用 `.\.venv\Scripts\python.exe`，不能少写 `.` 或反斜杠。手动运行后端时，数据库端口、密码必须以实际运行的 PostgreSQL 容器为准，不能直接照抄 `.env.example`。
- 补充：若 Uvicorn 在导入应用时提示缺少 `DATABASE_URL`，先检查 `backend\.env`，不要把它误判为 Uvicorn 或 Python 环境故障。可从已运行的 `streamhub-postgres` 容器读取真实 PostgreSQL 环境变量，对密码做 URL 编码后生成本机 `.env`；不得在终端、报告或公开仓库暴露密码。
- 补充：自动取容器密码不等于可以重置主库。普通本地后端使用现有 `streamhub`；当前 `SE2026` 本地 pytest/E2E 使用专用 `streamhub_test`；CI job 中名为 `streamhub` 的数据库属于一次性隔离容器。测试脚本必须校验环境和数据库名，且只允许重建专用或一次性测试库。更换 PostgreSQL 容器后若 `streamhub_test` 消失，应重新创建测试库，绝不能把会 DROP/CREATE schema 的测试临时改连演示主库。
- 补充：浏览器的 `http://localhost:5173` 与 `http://127.0.0.1:5173` 是不同 CORS Origin。登录界面显示“账号或密码错误”时，先看控制台是否为预检请求被拦截，并直接请求后端区分凭据错误和网络/CORS 错误。

## Windows 脚本先验证编码与目标 PowerShell 版本

- 纠错：`.bat` 中的中文提示被 CMD 当作命令执行时，不应误判为项目启动命令本身失效。
- 有效方法：先直接执行脚本里的实际启动命令；需要保留脚本时，再将其另存为 ANSI/GBK，或改为只含 ASCII 的输出。
- 补充：UTF-8 无 BOM 的 `.ps1` 在 Windows PowerShell 5.1 中也可能把中文字符串或路径读成乱码，严重时造成未闭合引号和 ParserError。门禁源码优先只含 ASCII；必须生成中文目录名时，可在运行时用 Unicode 码点构造。
- 补充：不能只用 PowerShell 7 验证脚本。交付前用 Windows PowerShell 5.1 的 `System.Management.Automation.Language.Parser.ParseFile` 检查 0 个解析错误，并至少执行一次真实冒烟。
- 补充：Windows PowerShell 5.1 在 `$ErrorActionPreference='Stop'` 下可能把 pytest/OpenCV 写入 stderr 的非致命输出包装成 `NativeCommandError`。调用原生命令时临时设为 `Continue`、保存 `$LASTEXITCODE`，结束后恢复原设置；测试是否失败以进程退出码为准。
- 补充：聊天或文档里显示的 `>` 可能是 PowerShell 续行提示符，不是需要复制的命令。误复制重定向符曾生成 `--min-total` 和 `erslausuDesktopSE2026` 文件。后续优先提供单行可复制命令；不要复制 `PS C:\...>` 或续行提示符。出现陌生未跟踪文件时，先检查大小和内容，确认是重定向产物后再按精确路径删除。

## WebSocket 测试按实际协议顺序断言

- 纠错：首个 WebSocket 消息不是固定 `join_ack`，本项目先发送 `online`。
- 有效方法：阅读服务端发送顺序；等待目标事件或允许已知事件序列，不能假定首条消息。

## Playwright 文本选择器要有业务上下文

- 纠错：全页 `getByText('审核中')` 或消息正文会命中多个元素，严格模式失败。
- 有效方法：审核状态限定到包含视频标题的 `tr`；直播消息限定到“直播间聊天”面板。

## 切换账号同时清持久态和内存态

- 纠错：只清 `localStorage` 后，Zustand 内存态仍保留旧账号。
- 有效方法：移除 `auth-storage` 后执行 `goto('/#/')`、`reload()`，并断言登录按钮出现，再登录下一个账号。

## WebSocket 关闭回调不能清掉新连接

- 纠错：旧 socket 的异步 `onclose` 可覆盖新 socket 状态，导致 UI 显示已连接但发送函数拿到 `null`。
- 有效方法：关闭回调先比较 `get().ws === ws`，只允许当前 socket 清状态；已由 E2E 直播弹幕流程验证。

## 测试报告只写真实执行结果

- 纠错：不能把文档中的计划测试或未单列单元测试写成已通过。
- 有效方法：运行完整官方测试命令后再填写报告。单个端到端用例分别通过，不代表整套端到端测试稳定通过；若全量运行出现超时或失败，必须如实记录，不能用旧 XML 或分开运行的成功结果覆盖。
- 补充：判断“测试通过”前，先确认浏览器、接口和数据库实际属于当前源码目录。端口可被另一份项目占用；一次测试即使变绿，也可能有步骤误调旧后端。E2E 应使用隔离端口、自己启动的服务，并在测试代码中避免把 API 地址硬编码到旧端口。
- 补充：完整 E2E 至少连续运行两轮。首次绿灯若伴随后端未处理异常、错误运行时或错误服务地址，不能作为最终验收；应先写回归测试复现并修复，再重新跑全套。
- 补充：先检查用户明确提到的所有报告目录，再汇总完成度。没有检查到的目录只能说“未核实”，不能据此下结论。
- 补充：受控失败是验证测试和门禁的有效方法，但要明确标记为“预期失败”，保留证据、立即恢复代码，并且不计入最终通过版本的失败数。
- 补充：历史受控失败依赖端口、进程和 CORS 等运行时状态；项目迁移或服务重启后不能直接复述旧结论。先探测当前服务归属和响应；若原故障前提已消失，改用稳定、无副作用的故障注入（如不可连接的测试后端）重新留证。
- 补充：新增 E2E spec 后，旧门禁中的裸 `npx playwright test` 会扩大到新旧全部测试。历史复跑要显式传测试文件或 grep；报告必须区分“旧 3 条”与“完整 11 条”，并标注日期和阶段。
- 补充：阶段报告中的“后续测试未开始”可以作为当时快照保留，但最终 README/交接必须明确最新状态。文档中的复跑命令也要当作可执行代码验证，同时检查 Markdown 本地链接和 JUnit 数字，避免文档正确但命令失效。
- 补充：用例范围必须先由当前正式清单、最新追溯表和用户确认共同冻结。历史报告、旧测试目录或微服务规划出现 UC09–UC18，不等于它们仍是正式用例；本项目当前中期范围只有 UC01–UC08。

## 测试用例数和测试点数分开统计

- 纠错：老师要求前后端合计至少 200 个测试点，不等于必须新增到 200 个测试用例。用户明确要求保持当前测试用例清单不变，并把失败路径计入测试点。
- 有效方法：冻结测试用例基线，再按独立输入、断言、权限、状态转换、边界和失败路径拆分测试点；门禁同时检查“用例数量未变化”和“测试点达到下限”，避免通过复制空壳用例凑数。
- 当前已验证基线：后端测试函数 29、pytest 收集项 38、E2E 场景 3；测试点为单元 60、API/集成 107、E2E 60、其中失败路径 29、总计 227。报告由 `scripts/test_point_report.py` 生成，不能手工改数字代替脚本核验。

## 新会话先恢复交接上下文

- 纠错：同一课程可能有多份项目副本、历史报告和未提交修改；不先读交接会误用旧路径、覆盖旧材料或重复排查。
- 有效方法：任何实作前先完整阅读最新 `HANDOFF.md` 与 `错题本.md`，列出实际源码目录、报告目录、端口、当前阶段和不可触碰的历史材料；用户列出多个目录时逐个核验。
- 补充：用户要求“材料放在三个报告目录”时，要说明范围。报告、门禁和证据放在报告目录；测试源码与生产修复仍应放在正常源码目录。不能为了迎合而声称“所有东西都在三个文件夹”。
- 补充：回答“以前的测试有没有动”前先做目录审计，并区分“旧核心文件未覆盖”和“旧目录出现了新增调试文件”。发现乱码、重复或临时证据时先报告，未经用户授权不得删除。
- 补充：用户明确要求“只看仓库”时，应把远程 GitHub 指定分支作为唯一代码事实来源。本地克隆只可用于用户明确授权的本地工作，不能用落后的本地 HEAD 覆盖远程结论。
- 补充：远程仓库会在会话中变化。用户说“已经上传”“现在再看”后，必须重新获取远程 HEAD、文件树和最新 Actions，并在结论中注明 commit SHA 或运行编号，不能复述缓存状态。

## 用户更新交付方式后贯穿剩余同类任务

- 纠错：用户在首个阶段后说“截图不用了，我自己来”，后续测试仍不应继续准备截图；用户又说“直接做下一个”“继续”后，也不应继续沿用旧交接中的逐阶段停顿。
- 有效方法：把最新明确指令视为当前同类任务的默认约束，并立即应用到所有剩余阶段：只保留测试结果、日志、JUnit 和报告，截图由用户负责；阶段通过后自动继续。只有破坏性操作、权限扩大、需求歧义会改变结果或真实阻断时才暂停。

## 按当前任务阶段报告缺口

- 纠错：CI/CD、部署等后续已排期任务不能被写成当前“测试任务失败”；反过来，也不能因计划以后再做就把它写成已完成。
- 有效方法：报告分成“本阶段已验证事实”“本阶段边界”“下一阶段计划”。例如本地门禁已经证明失败会退出并跳过发布/部署；未搭真实 CI 时，只能写“远端流水线未验证”。
- 补充：本地 CI 契约 12/12、Compose 运行和测试通过，只能证明配置结构与本地环境；没有 Push 新版工作流、没有新版 GitHub Actions run/Artifact 时，不能写“CI/CD 已跑通”。当前部署目标是 GitHub 临时 `ubuntu-latest` Runner 内的 Kind，任务结束即销毁；它符合课程允许的 Kind，但不是长期服务器或公开链接。

## 中期验收材料区分当前实现与下一阶段设计

- 纠错：不能把微服务接口清单中的私信、关注、举报、敏感词等规划能力当成当前正式用例或已实现功能；本项目当前确认范围为 UC01–UC08。
- 有效方法：在微服务章节开头明确写“下一阶段目标设计，不代表当前已经全部实现”；当前功能完成度只按 UC01–UC08 的运行、图、追溯和测试证据评价。
- 有效方法：文件夹缩略图只能证明文件存在，不能证明追溯完整。验收材料应展示一张可读的 UC01–UC08 对照表或追溯表。
- 有效方法：CI/CD 不是第四种测试。单元、集成/API、E2E 是质量门禁，流水线负责依次运行测试、构建版本镜像、部署和健康检查；任一测试失败时停止后续部署。
- 有效方法：判断 GitHub Actions 时先看 run/job 的 `conclusion`，再看 annotations。黄色 Node.js 运行时升级警告不等于测试失败。
- 有效方法：本地 Compose 的 `latest` 标签与正式 CI/Kubernetes 镜像版本分开审查；正式交付应展示 Git 短 SHA 等可追溯版本号。

## 用户上传媒体与业务数据分开持久化

- 纠错：演示视频、用户上传文件和数据库记录是三类不同数据，不能只回答“图片视频在 public”或把 MinIO称为第二个数据库。
- 有效方法：PostgreSQL 保存用户、标题、审核状态和媒体 URL；MinIO 保存用户上传的视频、封面和头像对象；仓库只保存代码、固定演示资源和部署配置。后端同时连接一个正式 PostgreSQL和一个 MinIO对象存储，并非连接两个正式数据库。
- 有效方法：旧媒体迁移采用幂等复制，确认对象存在前不删除本地原件；Git 索引删除与磁盘删除必须分开说明。验收时完成一次真实上传，再核对 PostgreSQL URL、MinIO对象和重启持久化。查询 `/uploads/%` 为 0 行时先确认是否真的上传过，不能直接判定迁移失败。
- 有效方法：MinIO Compose 容器名可能带临时哈希，查询状态和环境使用 `docker compose ps -q minio`；不要硬编码容器名。不得执行 `docker compose down -v`，除非用户明确授权删除 PostgreSQL和 MinIO数据卷。

## 本地修改、暂存、提交和推送必须分别授权

- 纠错：用户说“我自己测试，不推”后，不能因为已经执行最终检查或 `git add` 就继续准备 commit/push。`git add` 只是本地暂存，不会改变远程仓库，也不代表用户授权提交。
- 有效方法：每次汇报明确分成工作区修改、暂存、commit、push、远程 Actions 五个状态。只有用户明确说“提交并推送”时才能进入远程操作；用户只授权本地时，测试完成后应停止。
- 有效方法：用户限定只能修改两个文件时，收尾技能也不能顺手更新规则文件、清理测试目录或暂存文档；其他内容只读检查，破坏性清理必须另行获得授权。
