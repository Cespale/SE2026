# P5（CI/CD + 清雷）工作记录与使用说明

> 责任人：P5　完成日期：8/25
> 内容：清掉仓库里违反任务书的密钥/Token + 搭好 GitHub Actions 流水线 + K8s 部署清单 + 部署/健康检查脚本。

---

## 一、我做了什么（改动清单）

### 1. 清雷：清掉仓库里的硬编码密钥 / Token / 口令

任务书规定「仓库不能出现密码/Token/数据库口令」，下面这些都是原来违规、现在已清掉的：

| 问题 | 位置 | 处理 |
|---|---|---|
| 64 位 `\restrict`/`\unrestrict` token | `backend/docker-init/01_streamhub_backup.sql` | 删除这两行 |
| Supabase anon key（JWT）死代码 | `src/supabase/` 整个目录 | 删除（含 client.ts、types.ts） |
| 后端默认 `SECRET_KEY` | `backend/app/security.py` | 改成必须从环境变量读，没设置直接报错 |
| docker-compose 里的明文 `123456` 密码 + `SECRET_KEY` | `docker-compose.yml` | 改成 `${POSTGRES_PASSWORD}` / `${SECRET_KEY}` |
| `.env.example` 里的真实密码/密钥 | `backend/.env.example` | 改成 `CHANGE_ME` 占位 |
| 前端写死 `127.0.0.1:8000` | `src/api.ts` + `webpack.config.js` | 支持 `REACT_APP_API_BASE_URL` 注入，部署时可改 |
| `requirements.txt` 重复条目 | `backend/requirements.txt` | 去重 |

### 2. 新增 CI/CD 流水线

`.github/workflows/ci.yml`，严格按任务书顺序执行：

```
取代码 → 装依赖 → 编译 → 单测 → 集成 → 制作镜像 → 部署 K8s → 健康检查
```

分成 3 个 job，用 `needs` 串联：
- **job 1** 后端测试（pytest 单元 + 集成，自带 postgres 服务）
- **job 2** 前端（`npm ci` → 类型检查 → 编译 → `npm test`）
- **job 3** 制作镜像 + 部署 K8s + 健康检查（**只有 job1、job2 都通过才执行**）

关键点：
- **测试失败，流水线立即停**（不会制作镜像、不会部署）。
- 镜像用 git 短 SHA 当版本号，**禁止 latest**。

### 3. 新增 K8s 部署清单（`k8s/`）

| 文件 | 内容 |
|---|---|
| `postgres.yaml` | PostgreSQL 部署 + Service，用 ConfigMap 挂初始化 SQL |
| `backend.yaml` | 后端部署 + Service，密钥从 Secret 读 |
| `frontend.yaml` | 前端部署 + NodePort Service（30000 端口） |

### 4. 新增部署/健康检查脚本（`scripts/`）

| 文件 | 作用 |
|---|---|
| `deploy.sh` | 生成 Secret/ConfigMap → 应用清单 → 等 Deployment 就绪 |
| `health-check.sh` | port-forward + curl 检查后端 `/openapi.json`、前端 `/` |

### 5. 本地验证结果

- ✅ 前端 `tsc --noEmit` 通过
- ✅ 前端 `webpack` 生产构建成功（4 个 warning 全是原有的，不影响运行）
- ✅ `docker compose config` 校验通过，环境变量正确从 `.env` 读取

---

## 二、你要怎么用

### 步骤 1：本地把系统跑起来（先确认没改坏）

```bash
# 首次克隆后：复制环境变量模板（.env 已被 gitignore，不会提交）
cp .env.example .env        # Windows 命令行用：copy .env.example .env

# 编辑 .env，把 CHANGE_ME 换成你自己的值（本地我建好了一份，可直接用）
# POSTGRES_PASSWORD=123456
# SECRET_KEY=随便一串字符

# 启动三容器
docker compose up -d --build

# 看日志确认都起来了
docker compose ps
```

启动后访问：
- 前端：http://localhost:5173
- 后端：http://localhost:8000/docs （FastAPI 自动文档）

### 步骤 2：给 GitHub 仓库配 Secrets（CI 部署阶段要用）

1. 打开 `https://github.com/Bolic137/SE2026/settings/secrets/actions`
2. 点 **New repository secret**，加两个：
   - `POSTGRES_PASSWORD` → 比如 `123456`
   - `SECRET_KEY` → 随便一串字符

> 不配这两个，流水线部署阶段会报「需要设置 POSTGRES_PASSWORD/SECRET_KEY」。

### 步骤 3：提交代码、推上去触发流水线

```bash
git add .
git commit -m "P5: 清理硬编码密钥 + 添加 CI/CD 流水线 + K8s 部署清单"
git push origin main
```

推到 main 后，打开仓库 **Actions** 标签页，就能看到流水线跑起来。三个 job 全绿 = 通过。

> ⚠️ 现在推上去，job1/job2 会**红**，这是正常的：因为 `pytest`（P4 写）和 `npm test`（P2 写）还没测试代码。等他们写完测试，流水线才会绿。红反而证明「测试失败会停流水线」是有效的。

### 步骤 4（可选）：本机验证 K8s 部署

需要有 Docker Desktop + kubectl + Kind（或 minikube），装好后：

```bash
# 起一个本地 K8s 集群
kind create cluster

# 打镜像（带版本号）
docker build -f backend/Dockerfile -t streamhub-backend:test .
docker build -f Dockerfile.frontend -t streamhub-frontend:test .

# 把镜像塞进集群
kind load docker-image streamhub-backend:test
kind load docker-image streamhub-frontend:test

# 部署（脚本会自动读 .env 里的密钥）
export VERSION=test
bash scripts/deploy.sh

# 健康检查
bash scripts/health-check.sh
```

看到 `全部健康检查通过` 就说明部署成功了。前端可以通过 NodePort 访问：http://localhost:30000

### 步骤 5：队友要配合的事

- **P4** 写后端测试，命令用 `pytest`（我已在流水线里定死）。
- **P2** 写前端 E2E，并且给 `package.json` 加一个 `test` 脚本（比如 `"test": "playwright test"`），否则 `npm test` 会报「Missing script」。

---

## 三、新增/修改文件一览

**新增：**
- `.github/workflows/ci.yml`
- `k8s/postgres.yaml`、`k8s/backend.yaml`、`k8s/frontend.yaml`
- `scripts/deploy.sh`、`scripts/health-check.sh`
- `.env.example`（根目录，占位模板）
- `.env`（根目录，本地开发用，**已 gitignore，不要提交**）

**修改：**
- `backend/docker-init/01_streamhub_backup.sql`（删 token）
- `backend/app/security.py`（SECRET_KEY 强制走环境变量）
- `backend/requirements.txt`（去重）
- `backend/.env.example`（占位）
- `docker-compose.yml`（密码/密钥走环境变量）
- `.gitignore`（加 `.env` / `.env.local`）
- `src/api.ts`（API 地址可注入）
- `webpack.config.js`（加 DefinePlugin）

**删除：**
- `src/supabase/` 整个目录（死代码 + anon key）
