# 第 9 组 StreamHub —— 前 5 天分工与任务（8/25–8/29）均衡版【5 人小组修改版】

> 前 5 天总分 40 分，8/29（周六）中期检查。核心目标：**把上学期系统跑起来 + 补文档 + 补测试 + 容器化 + 跑通 CI/CD + 定微服务拆分方案**。
>
> 原本 6 个角色，现调整为**5 人均衡分配**，合并部分低耦合任务，兼顾技术难度、工作量，避免某一人负担爆炸。看完各自认领（可报第一 / 第二志愿），认领后在群里接龙回复：**「姓名 + 角色编号」**。

------

## 开工前现状（重要，影响工作量判断）

- ✅ Dockerfile（前端 / 后端）、docker‑compose、数据库迁移脚本**已经存在**，不用从零写，但**尚未验证能否启动**。

- ✅ `assets/` 已有上学期 3 份 PDF（需求 / 概要 / 开发计划）+ 5 份设计 MD，文档岗是**补**而不是从零。

- ❌ **目前仓库里没有真正的测试**（`test.js`/`test_simple.js` 是未提交的临时脚本），单元 / 集成 / E2E 都要**从零写**。

- ❌ **没有任何 CI 配置**，流水线要**从零搭**。

- ⚠️ 

  仓库里有硬编码密钥 /token，违反任务书「仓库不能出现密码 / Token」

  ，第 1 天必须清：

  - `backend/docker‑init/01_streamhub_backup.sql` 第 5 行的 `\restrict <64位token>`（非法 psql 指令，会导致 postgres 初始化报错）；
  - `src/supabase/client.ts` 里的硬编码 anon key（Supabase 是模板残留死代码，可整体删掉）；
  - `backend/app/security.py` 的默认 `SECRET_KEY`，改由 `.env` 注入。

- ⚠️ **前端 7 处硬编码 `http://localhost:8000`**，且 webpack 没有注入 `process.env`，部署 / CI 时改不动，需要统一成 `API_BASE`。

- ⚠️ **当前工作目录还没接 Git**（需 `git init` + 关联 GitHub remote + checkout main），否则打不了 `monolith‑start` 标签、CI 无法触发。

------

## 五个角色（5 人均衡版）

> 调整说明：原 6 角色 P2、P3 两份文档任务重新拆分；组长卸下部分文档压力；原 P4、P5 测试岗位保留；原 P6 CI/CD 保留；将追溯表分摊，整体平衡难度与工作量，消除单人全组最重的情况。

表格

| 编号   | 角色                               | 前 5 天要做什么                                              | 技术难度   | 工作量  | 适合谁                                         |
| ------ | ---------------------------------- | ------------------------------------------------------------ | ---------- | ------- | ---------------------------------------------- |
| **P1** | 组长 + 容器化                      | ① 每天主持站会、写 200 字简报、维护看板、截图存档；② 第 1 天接 Git、列业务场景（用例）清单交老师确认、打`monolith‑start`标签；③ 中期检查输出**微服务拆分方案**（服务划分图、接口清单、数据表归属表）；④ 容器化：验证 / 修补 Dockerfile、compose、DB 脚本，清除`\restrict`token，保证三容器可启动、DB 自动初始化、README 可复现 | 中–高 ★★★★ | 中 ★★★  | 愿意牵头、会 Docker、看得懂后端架构            |
| **P2** | 文档岗：需求 + 概要 + 部分图       | 编写《软件需求规格说明书》《软件概要设计说明书》；绘制用例图、用例说明、概念类图、组件图；输出系统级、组件级顺序图；输出统一版本的**用例编号全集**，交付 P3/P4/P5 使用 | 低‑中 ★★   | 高 ★★★★ | 细心、会写文档、会画 UML                       |
| **P3** | 文档 + 测试追溯：详细设计 + 追溯表 | 编写《软件详细设计说明书》；绘制类图、对象级顺序图；**维护完整追溯表**（需求‑用例‑三层模型‑代码‑测试一张表），对接 P2 文档、P4/P5 测试，保证编号一一对应 | 中‑高 ★★★★ | 高 ★★★★ | 熟悉项目代码，能画类图、对象级顺序图           |
| **P4** | 测试岗：单元 + API 集成 + E2E      | 使用 pytest 完成后端单元、API 集成测试（FastAPI TestClient），覆盖主流程、分支、异常；使用 Playwright 完成核心业务 E2E 测试；汇总生成完整测试报告（总数 / 通过 / 失败 / 原因 / 环境） | 中‑高 ★★★★ | 高 ★★★★ | 会 Python、pytest，了解 TS/JS，会简单 E2E 测试 |
| **P5** | CI/CD + 公共前置清理               | ① 从零搭建 GitHub Actions 流水线：取码→装依赖→编译→单测→集成→制作镜像→部署 K8s→健康检查，测试失败必须终止流水线；② 提交流水线配置、K8s 部署文件、脚本；镜像使用固定版本号，禁止 latest；③ 参与第一天公共风险清理：清理硬编码密钥、删除 supabase 残留代码，配合 P1 修改前端硬编码[localhost](https://localhost)地址，统一为 API_BASE | 高 ★★★★★   | 高 ★★★★ | 熟悉 GitHub Actions、Docker、K8s               |

> 说明：
>
> 1. 原 P4 单元集成、P5 E2E 合并为 P4，E2E 只要求**核心业务用例全覆盖**，非全部用例，降低压力；
> 2. 原 P6 CI/CD 改为 P5，把仓库密钥清理、前端地址改造公共前置任务交给 P5，和容器化 P1 协同；
> 3. P1 组长不再包揽文档相关工作，只保留项目管理 + 容器化 + 微服务方案，工作量下降；
> 4. P2 产出统一用例编号，是全组协作基础，P3 负责追溯表对齐文档与测试，保证中期检查一一对应要求。

------

## 5 天时间线（谁哪天做什么）

表格

| 日期                     | 关键动作                                                     | 主要责任                                                     |
| ------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **8/25（第 1 天）**      | 接 Git + 清 token / 密钥 → 启动原系统 → 逐项检查用例 → 列用例清单交老师确认 → 打 `monolith‑start` 标签 → 建看板拉老师 / 助教 | 全组启动；P1 主导接 Git、清单、标签、看板；P5 配合完成密钥、硬编码地址清理 |
| **8/26–27（第 2–3 天）** | 三条线并行：P2 写需求概要文档画图输出统一用例编号；P3 做详细设计 + 追溯表；P4 编写单元、API、E2E 测试；P5 搭建 CI 流水线骨架；P1 验证容器、输出微服务拆分方案 | 各自开工，互相对齐用例编号                                   |
| **8/28（第 4 天）**      | CI 流水线完整跑通（push 自动构建测试）；微服务方案定稿；测试报告完成；追溯表完成对齐校验 | P5 调通流水线；P1 完善微服务方案；P4 输出测试报告；P3 校验追溯表 |
| **8/29（第 5 天）**      | **中期检查**：核对 ①系统能启动 + 用例可跑 + 存在标签 ②三层图 + 追溯表齐全 ③容器正常启动 + CI 可构建测试 ④微服务方案完整 | 全组逐项核对自查                                             |

------

## 协作约定（避免各干各的）

1. **统一用例编号**：P2 输出一套标准用例编号。P3 的图（`SYS‑SEQ/COMP‑SEQ/OBJ‑SEQ`）、P4 的测试（`INT‑TC/E2E‑TC`）、P3 的追溯表全部复用同一套编号，否则中期检查 “一一对应” 无法通过。
2. **测试命令契约**：P5 先定死 `pytest`（后端）和 `npm test`（前端）两个命令，P4 按照这套命令编写测试，互不阻塞。
3. **测试失败必须停**：流水线任意步骤失败（尤其测试），禁止继续构建镜像、部署；P4、P5 提前约定退出码与失败信号。
4. **看板每天更新**：P1 维护看板，**每个人自己的任务卡片自行填写实际完成时间、证据链接**，每日截图存档。
5. **第 1 天先清雷**：`\restrict` token、Supabase 死代码、硬编码密钥、前端硬编码`localhost:8000`属于公共前置障碍，P1 与 P5 协同清理，否则容器化、CI 后续全部会被阻塞。